import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#224BCF",
            light: "#18ACDB"
        },
        secondary: {
            main: "#FA47A4",
            light: "#C63AFD"
        }
    }
});